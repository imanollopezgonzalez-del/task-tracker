# Client ID de la credencial OAuth 2.0 (Web application) creada en Google Cloud Console.
# No es secreto (va también al frontend), pero la función lo necesita para armar el OAuth2Client.
# Copiar a functions/.env y completar con el valor real.
GOOGLE_OAUTH_CLIENT_ID=
